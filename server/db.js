// Database disabled for local development - using memory storage
console.log("Using local memory storage instead of database");

// Mock database exports for compatibility
export const pool = null;
export const db = null;